name: Validate project

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Validate Rainmeter and project structure
        shell: pwsh
        run: ./tools/validate-project.ps1
